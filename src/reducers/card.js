const initialState = {

}

const cardReducer = (state = initialState, action) => {
  switch (action) {
    case 'k':
      
      return {};
  
    default:
      return {};
  }
}

export default cardReducer;